# R&R 정리판 구축 및 운영

## 1. 프로젝트 개요

전사 구성원이 업무의 범위와 담당자를 명확히 확인하고, 팀장이 적합한 담당 팀을 배정·관리하는 업무 운영 도구를 구축한다.

- 사용 대상: 전사 구성원
- 권한: 시스템 관리자 / 팀장 / 팀원
- 로그인: 60일 운영 기간에는 사용자 선택 방식으로 시작하고, 이후 회사 로그인 연동을 확장한다.
- 핵심 원칙: 모든 핵심 업무는 담당자 또는 담당 팀을 가지며, 주담당자·공동담당자를 구분한다.

## 2. 주요 KPI

| KPI | 목표 | 산출 주기 |
|---|---:|---|
| 핵심 업무 R&R 등록 및 책임자 지정률 | 100% | 실시간·주간 |
| 업무 등록 후 담당자 지정까지 평균 시간 | 1영업일 이내 | 주간 |
| 업무 중복·누락 건수 | 30% 감소 | 주간·60일 비교 |
| 업무 배정 적합도 | 5점 만점 중 4점 이상 | 주간 |

## 3. 저장 데이터 모델

### 3.1 KPI

#### `kpi_metric`

| 칸 이름 | 설명 |
|---|---|
| `id` | 지표 ID |
| `name` | 지표명 |
| `kind` | 정량·정성 구분 |
| `unit` | %, 일, 점 등 단위 |
| `target` | 목표값 |
| `method` | 산출 방법 |

#### `kpi_entry`

| 칸 이름 | 설명 |
|---|---|
| `id` | 입력 ID |
| `metric_id` | 지표 ID |
| `value` | 정량 실적값 |
| `recorded_on` | 기록일 |
| `note` | 근거·메모 |

#### `kpi_note`

| 칸 이름 | 설명 |
|---|---|
| `id` | 기록 ID |
| `metric_id` | 지표 ID |
| `content` | 정성 기록 |
| `photo_url` | 사진 주소 |
| `recorded_on` | 기록일 |

### 3.2 구성원·업무

#### `rr_member`

| 칸 이름 | 설명 |
|---|---|
| `id` | 구성원 ID |
| `name` | 이름 |
| `level` | 직급 |
| `skills` | 보유 역량 배열 |
| `experience` | 관련 경력 연수 |
| `task_types` | 수행 가능 업무 종류 |
| `capacity_hours` | 주당 R&R 업무 가능시간 |
| `availability` | 현재 가용 비율 |
| `max_workload` | 배정 허용 상한 |
| `workload` | 업무에서 자동 계산한 부하율 캐시 |
| `profile_source` | 직급 기본값·개인 설정 구분 |

#### `rr_task`

| 칸 이름 | 설명 |
|---|---|
| `id` | 업무 ID |
| `title` | 업무명 |
| `description` | 업무 설명 |
| `created_by` | 등록자 |
| `owner_name` | 주담당자 호환용 이름 |
| `due_on` | 기한 |
| `status` | 미배정·배정·진행·막힘·완료 |
| `task_type` | 업무 종류 |
| `difficulty` | 난이도·필요 수준 |
| `required_level` | 필요한 최소 직급 |
| `required_skills` | 필요한 역량 배열 |
| `min_experience` | 최소 경험 연수 |
| `estimated_hours` | 예상 소요시간 |
| `collaborators` | 협업 부서 배열 |
| `assignment_mode` | 단독·다중 협업 |
| `required_assignees` | 필요한 담당 인원 |
| `created_at` | 등록일시 |
| `assigned_at` | 최초 담당자 지정일시 |
| `assignment_hours` | 등록 후 담당자 지정까지 걸린 시간 |

#### `rr_task_assignee`

| 칸 이름 | 설명 |
|---|---|
| `id` | 배정 ID |
| `task_id` | 업무 ID |
| `member_name` | 담당 구성원 |
| `role` | 주담당·공동담당 |
| `assigned_by` | 배정자 |
| `assigned_at` | 배정일시 |

#### `rr_assignment_history`

| 칸 이름 | 설명 |
|---|---|
| `id` | 이력 ID |
| `task_id` | 업무 ID |
| `previous_owners` | 변경 전 담당 팀 |
| `new_owners` | 변경 후 담당 팀 |
| `reason` | 배정 변경 사유 |
| `changed_by` | 변경자 |
| `changed_at` | 변경일시 |

#### `rr_task_edit_history`

| 칸 이름 | 설명 |
|---|---|
| `id` | 수정 이력 ID |
| `task_id` | 업무 ID |
| `changed_fields` | 수정된 칸 목록 |
| `previous_data` | 수정 전 값 |
| `new_data` | 수정 후 값 |
| `reason` | 수정 사유 |
| `changed_by` | 수정자 |
| `changed_at` | 수정일시 |

### 3.3 인수인계·만족도·의사결정

#### `rr_handover`

| 칸 이름 | 설명 |
|---|---|
| `id` | 인수인계 ID |
| `task_id` | 업무 ID |
| `from_member` | 기존 담당자 |
| `to_member` | 신규 담당자 |
| `current_status` | 현재 업무 현황 |
| `pending_items` | 미결사항 |
| `next_actions` | 후속 조치 |
| `related_contacts` | 관련 담당자·부서 |
| `handover_note` | 인수인계 메모 |
| `handed_over_at` | 인수인계 일시 |
| `confirmed_at` | 신규 담당자 확인일시 |

#### `rr_task_satisfaction`

| 칸 이름 | 설명 |
|---|---|
| `id` | 만족도 ID |
| `task_id` | 업무 ID |
| `owner_name` | 업무의 주담당자 |
| `submitted_by` | 실제 응답자 |
| `fit_score` | 업무 수준 점수 1~5 |
| `load_score` | 업무 부하 점수 1~5 |
| `challenge_score` | 더 높은 난이도 수용도 1~5 |
| `comment` | 추가 의견 |
| `recorded_on` | 응답일 |

#### `rr_decision_log`

| 칸 이름 | 설명 |
|---|---|
| `id` | 의사결정 ID |
| `task_id` | 업무 ID |
| `reviewer` | 검토자 |
| `approver` | 승인자 |
| `final_decider` | 최종 결정권자 |
| `decision` | 결정 내용 |
| `decision_note` | 결정 근거 |
| `decided_at` | 결정일시 |

#### `rr_task_quality_review`

| 칸 이름 | 설명 |
|---|---|
| `id` | 품질 점검 ID |
| `task_id` | 업무 ID |
| `issue_type` | 중복·누락 |
| `status` | 미해결·해결 |
| `detected_on` | 발견일 |
| `resolved_on` | 해결일 |
| `reviewer` | 점검자 |
| `note` | 점검 메모 |

## 4. 화면 목록

| 화면 | 보여주는 내용 | 입력·행동 |
|---|---|---|
| 사용자 선택·접속 | 구성원 목록·현재 선택 사용자 | 사용자 선택 |
| 현황판 | KPI, 배정률, 업무 추이, 미배정·기한 임박 업무, 팀 부하 | 업무 등록, 업무판 이동, KPI 확인 |
| 업무 항목 | 업무 카드, 상태, 담당 팀, 기한, 역량 조건, 협업 부서 | 업무 등록·수정, 상태 변경, 자동·수동·다중 배정 |
| 업무 상세 | 업무 조건, 담당 팀, 변경 이력, 추천 후보, 의사결정 정보 | 팀장 수정, 담당자 조정, 상태 전환, 인수인계 등록 |
| 내 업무 | 본인이 주담당·공동담당인 업무, 기한, 상태 | 상태 업데이트, 완료 처리, 만족도 입력 |
| 인수인계 | 진행 업무, 미결사항, 후속 조치, 관련 담당자 | 인수인계 작성·확인 |
| 팀원 프로필 | 직급, 역량, 가능 업무, 자동 계산 부하, 담당 업무 | 팀장만 역량·기본값 수정 |
| KPI 기록 | 핵심 KPI, 주간 실적, 정성 기록, 사진 | 정량값·정성 내용·사진 입력 |
| 관리자 | 입력 누락, 업무 중복·누락 점검, 전체 배정 현황, 수정·삭제 이력 | 기록 수정·삭제, 품질 점검, CSV 다운로드 |
| 승인·의사결정 | 검토자·승인자·최종 결정권자, 결정 이력 | 의사결정 기록·조회 |
| 조직별 리포트 | 조직별 R&R 지정률, 배정시간, 적합도, 중복·누락 추이 | 기간·조직 필터, 리포트 조회 |
| 시스템 설정 | 조직·팀·직급 기본값, 권한, KPI 기준값 | 시스템 관리자 설정 |

업무 등록과 배정에는 별도 승인 단계를 두지 않는다. 승인·의사결정 화면은 예외적인 검토·결정 기록을 위한 용도로 운영한다.

## 5. 사용자 시나리오

### 일반 구성원

1. 사용자 선택 화면에서 본인을 선택한다.
2. 현황판에서 내 업무와 팀의 미배정·기한 임박 업무를 확인한다.
3. 모호한 업무가 있으면 업무명, 설명, 종류, 난이도, 기한, 예상시간, 협업부서를 입력해 등록한다.
4. 내 업무 화면에서 주담당·공동담당 업무의 상태와 기한을 확인한다.
5. 담당자 변경 시 인수인계 내용을 작성하고 신규 담당자의 확인을 받는다.
6. 업무 진행 후 본인만 업무 수준·부하·난이도 적합도를 평가한다.

### 팀장

1. 팀 업무 현황과 자동 계산 부하율을 확인한다.
2. 업무 조건에 맞는 자동 추천팀을 검토하거나 후보를 여러 명 선택해 수동 배정한다.
3. 첫 번째 담당자를 주담당, 나머지를 공동담당으로 지정한다.
4. 배정 전 확인 모달에서 담당 팀과 배정 후 예상 부하를 확인하고 확정한다.
5. 업무 범위·난이도·기한·협업부서가 바뀌면 수정 사유를 남기고 업무 항목을 수정한다.
6. 주간 관리자 화면에서 미배정, 중복·누락, KPI 누락 기록을 점검한다.

### 시스템 관리자

1. 조직별 팀과 팀장 권한을 관리한다.
2. 직급별 기본 역량·업무 가능량·KPI 기준값을 설정한다.
3. 조직별 리포트와 변경 이력을 확인한다.

## 6. KPI 산출 방법

### 6.1 핵심 업무 R&R 등록 및 책임자 지정률

- 기록: `rr_task`, `rr_task_assignee`
- 분모: 해당 주에 활성 상태인 핵심 업무 수
- 분자: 필수 업무 정보가 입력되고 `rr_task_assignee`에 주담당 또는 공동담당이 1명 이상 등록된 업무 수
- 산식: `분자 ÷ 분모 × 100`
- 목표: 100%

### 6.2 업무 등록 후 담당자 지정까지 평균 시간

- 기록: `rr_task.created_at`, `rr_task.assigned_at` 또는 `rr_task_assignee.assigned_at`
- 대상: 해당 주에 최초 배정된 업무
- 산식: `최초 배정일시 - 등록일시`의 영업일 평균
- 목표: 1영업일 이내

### 6.3 업무 중복·누락 건수

- 기록: `rr_task_quality_review`
- 기준: 팀장이 주간 점검에서 중복 또는 누락으로 등록한 미해결 건수
- 기준선: 운영 첫 2주간의 평균 건수
- 산식: `(기준선 - 현재 주 건수) ÷ 기준선 × 100`
- 목표: 60일 종료 시 기준선 대비 30% 감소

### 6.4 업무 배정 적합도

- 기록: `rr_task_satisfaction`
- 응답자: 해당 업무의 주담당자·공동담당자 본인만 입력
- 산식: `(fit_score + load_score + challenge_score) ÷ 3`의 주간 평균
- 목표: 5점 만점 중 4점 이상
- 유의사항: 같은 업무에 여러 담당자가 응답하면 담당자별 응답을 각각 1건으로 계산한다.

## 7. 60일 실행 계획

### 1~10일: 실사용 기반 정비

- 전사 구성원·팀장·시스템 관리자 권한 구성
- 직급별 기본 역량과 업무 가능량 등록
- 핵심 업무 목록과 필수 입력 기준 확정
- 사용자 선택 방식으로 접속·권한 흐름 검증

### 11~25일: 업무 등록·배정 운영

- 핵심 업무를 업무판에 전수 등록
- 자동 추천·수동 배정·다중 배정 운영
- 업무 등록 후 1영업일 이내 담당자 지정
- 팀장 업무 수정과 배정 변경 이력 기록

### 26~40일: 인수인계·의사결정 정착

- 담당자 변경 업무에 인수인계 기록 의무화
- 업무별 검토자·승인자·최종 결정권자 등록
- 중복·누락 주간 점검 시작
- 팀별 업무 부하와 담당 영역 확인

### 41~52일: KPI·관리 운영

- 실시간 현황판과 주간 KPI 기록 운영
- 업무 만족도 수집 및 배정 적합도 계산
- 관리자 화면에서 누락·중복·지연 업무 점검
- 조직별 리포트와 CSV 다운로드 검증

### 53~60일: 성과 검증·정착

- 4개 KPI의 기준선과 현재값 비교
- 미달 KPI의 원인 업무와 담당팀 재점검
- 팀별 사용 피드백 반영
- 표준 운영 절차와 다음 개선 backlog 확정

## 8. 이번 60일에 만들지 않을 것

- 모바일 앱
- 푸시 알림
- 메신저 연동

그 외 핵심 기능은 웹 기반 실사용 범위에 포함한다. 회사 SSO·인사시스템 연동은 사용자 선택 방식 운영 후 다음 단계로 확장한다.
