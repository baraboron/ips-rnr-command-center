# 다음 세션 시작용 프롬프트

아래 workspace의 R&R 정리판을 이어서 개발해줘.

먼저 다음 파일을 읽어라.

- `HANDOFF_다음세션.md`
- `R&R_정리판_실사용_기획서.md`
- `app.js`
- `supabase.sql`
- `index.html`
- `styles.css`

목표는 프로토타입을 실제 팀이 매일 쓰는 도구로 완성하는 것이다. 결론부터 짧게 보고하고, 한 번에 한 작업 단위씩 구현·검증해라. 기존 구현을 무너뜨리지 말고, 변경 전 현재 동작을 확인해라.

최우선 과제는 다음 순서다.

1. Supabase 테이블·RLS·화면 데이터 모델의 불일치를 점검하고 수정
2. localStorage fallback을 유지하되 업무·배정·상태·KPI·만족도·이력의 Supabase 영구 저장 구현
3. 권한 구현: 시스템 관리자, 팀장, 팀원
4. 인수인계, 의사결정 기록, 중복·누락 품질 기록 구현
5. 실제 데이터 기반 KPI 계산과 주간 snapshot 구현
6. UI 문구·UTF-8·날짜 처리·중복 함수 선언 정리

제품 규칙:

- 핵심 업무는 담당자 또는 공동 담당자가 반드시 지정되어야 한다.
- 자동 추천은 직급, 역량, 경험, 업무 유형, 난이도, 현재 진행 업무 기반 자동 업무부하를 함께 반영한다.
- 수동 배정도 확인 모달을 거치며, `1시간 동안 보지 않기` 선택 시에만 해당 사용자에게 1시간 동안 모달을 생략한다.
- 배정 전환, 상태 전환, 업무 수정은 기존 값을 덮어쓰지 말고 이력을 쌓는다.
- 상태 변경 모달의 필드명은 `변경/전환 이력`이며 빈 값 저장을 허용하지 않는다.
- 한 번 배정된 업무를 담당 미정으로 바꾸는 것은 관리자만 가능하다.
- 업무 만족도는 실제 담당자 본인만 입력할 수 있다.
- 업무부하율은 구성원이 임의 입력하지 않고 현재 진행 중 업무, 예상 시간, 개인별 가용시간·허용부하로 계산한다.
- 강조색은 `#0f4c81`, 보조색은 `#0b3860`, 배경은 `#e8eff5`다.
- 모바일 앱, 푸시 알림, 메신저 연동은 이번 범위가 아니다.

KPI 계산 기준:

- 책임자 지정률 = 활성 핵심 업무 중 담당자 1명 이상인 업무 / 활성 핵심 업무 × 100
- 평균 배정시간 = 최초 담당자 지정 시각 − 업무 등록 시각의 영업일 평균
- 중복·누락 감소율 = 1~2주 기준선 대비 현재 `rr_task_quality_review` 건수 감소율
- 업무 배정 적합도 = 실제 담당자의 적정성·부하·난이도 응답 평균, 목표 4/5 이상

작업 방식:

- 먼저 현재 상태와 문제를 3~5개 항목으로 보고
- 수정은 `apply_patch`로 진행
- 각 작업 후 `node --check app.js`와 필요한 브라우저·HTTP 검증 실행
- 저장 실패를 성공으로 표시하지 말 것
- 사용자가 바로 확인할 수 있도록 변경 파일과 검증 결과를 마지막에 요약
- 별도 요청 없이는 git commit, 외부 배포, 데이터 삭제를 하지 말 것

첫 작업은 `supabase.sql`과 `app.js`를 대조해 실제 영구 저장에 필요한 테이블·컬럼·저장 함수를 목록화하는 것부터 시작해라.
