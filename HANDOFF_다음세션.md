# R&R 정리판 다음 세션 핸드오프

## 1. 세션 목표

어제 만든 프로토타입을 실제 팀이 사용하는 업무 도구로 전환한다. 회사 전 범위에서 업무를 등록하고, 팀장이 직급·역량·경험·자동 계산 업무부하를 반영해 자동 또는 수동으로 한 명 이상 배정한다.

핵심 KPI:

- 핵심 업무 R&R 등록 및 책임자 지정률 100%
- 업무 등록 후 담당자 지정까지 평균 1영업일 이내
- 업무 중복·누락 건수 30% 감소
- 업무 배정 적합도 5점 만점 중 4점 이상

목표 W-WAY 항목: 업무지시, 업무인수인계, 문서작성, 의사결정.

## 2. 현재 프로젝트 위치

- 작업 폴더: `C:\Users\user\Documents\ChatGPT\원익Way IPS`
- 주요 파일: `app.js`, `index.html`, `styles.css`, `supabase.sql`
- 기획서: [R&R_정리판_실사용_기획서.md](C:\Users\user\Documents\ChatGPT\원익Way IPS\R&R_정리판_실사용_기획서.md)
- 로컬 실행 주소: `http://localhost:4173`
- 현재 저장 방식: 브라우저 `localStorage`가 기본이며, 환경변수가 있을 때 Supabase REST 동기화를 시도하는 fallback 구조
- 현재 테스트 기준: `node --check app.js`, 정적 서버 HTTP 200, 브라우저에서 주요 모달·배정 흐름 확인

## 3. 이미 구현된 범위

### 업무판

- 현황판형 대시보드와 딥오션 색상 체계(`#0f4c81`)
- 업무 등록, 검색, 상태·담당자 필터, 정렬
- 상태 타일, 상태별 강조, 카드 hover 확대, 부드러운 전환
- 진행 중 업무만 좌측 메뉴 카운트
- 업무 상세 모달
- 팀장만 업무 항목 수정 가능
- 업무 수정 시 수정 사유를 필수로 받고 이력 저장

### 담당 배정

- 업무 유형, 난이도, 필요 직급, 필요 역량, 최소 경험, 예상 시간, 협업 부서 설정
- 직급별 기본 역량·업무 유형·경험·허용 부하·가용시간 자동 적용
- 구성원별 세부 역량 설정 구조
- 현재 진행 중 업무와 예상 시간을 기준으로 업무부하 자동 계산
- 업무를 여러 명에게 공동 배정
- 자동 추천·자동 배정
- 수동 배정
- 배정 전 확인 모달, 1시간 동안 보지 않기 옵션
- 담당 변경 이력 보존
- 한 번 배정된 업무를 담당 미정으로 되돌리는 것은 관리자만 가능

### 만족도·관리

- 실제 담당자 본인만 업무 만족도 입력 가능
- 적정 수준, 부하, 더 높은 난이도 수용 여부 입력
- 팀원 프로필과 업무 목록·자동 업무부하 표시
- KPI 정량·정성 기록 입력 및 최근 입력 목록 표시
- 관리자 현황·담당 변경 이력·CSV 다운로드 화면

## 4. 다음 세션 우선 작업

우선순위 순서대로 진행한다.

1. `supabase.sql`과 화면 데이터 모델을 일치시킨다.
   - 현재 SQL에 없는 `rr_handover`, `rr_decision_log`, `rr_task_quality_review` 추가
   - KPI 원본·업무·구성원·배정·이력·만족도 테이블의 관계와 인덱스 정리
   - `rr_task_assignee`를 기준으로 다중 담당자 조회를 표준화

2. localStorage 중심 저장을 Supabase 영구 저장으로 전환한다.
   - 읽기: 앱 시작 시 Supabase 조회
   - 쓰기: 업무, 배정, 상태, 만족도, KPI 기록을 저장 후 화면 갱신
   - 네트워크 실패 시 사용자에게 저장 실패를 알리고 임의로 성공 처리하지 않기
   - 현재 사용자 권한을 Supabase RLS 정책으로 보강

3. 사용자·권한 흐름을 실제 사용 수준으로 만든다.
   - 현재 사용자 선택 방식은 60일 검증용
   - 역할: 시스템 관리자, 팀장, 팀원
   - 팀원: 업무 등록·본인 업무 상태·본인 만족도
   - 팀장: 업무 수정·담당 배정·상태 관리·팀 조회
   - 관리자: 전체 관리·담당 미정 전환·삭제·기록 수정/삭제·설정

4. 인수인계·의사결정 기능을 구현한다.
   - 담당자 변경 시 현재 상태, 미결사항, 후속 조치, 관련 담당자, 인수인계 확인
   - 검토자·승인자·최종 결정권자와 결정 기록
   - 일반 업무에 불필요한 승인 단계를 강제하지 말고 예외 업무에만 사용

5. KPI를 실제 기록 기반으로 계산한다.
   - 배정률: 활성 핵심 업무 중 담당자 1명 이상인 업무 / 활성 핵심 업무 × 100
   - 평균 배정시간: 최초 담당자 지정 시각 − 업무 등록 시각의 영업일 평균
   - 중복·누락 감소율: `rr_task_quality_review`의 1~2주 기준선과 현재 건수 비교
   - 적합도: 실제 담당자의 적정성·부하·난이도 응답 평균
   - 실시간 현황과 주간 snapshot을 함께 저장

6. 문구·인코딩·날짜를 정리한다.
   - `app.js`는 과거 함수 선언과 최신 함수 선언이 섞여 있으므로 중복을 제거
   - 현재 파일에서 한글이 깨져 보이는지 브라우저로 확인하고 UTF-8로 통일
   - `today` 하드코딩을 실제 날짜 함수로 교체
   - 관리자 이력의 다중 담당자 표시를 최종 데이터 구조에 맞게 수정

## 5. 반드시 지켜야 할 제품 규칙

- 모든 핵심 업무는 결국 담당자 또는 공동 담당자가 있어야 한다. 미배정 상태를 성과로 간주하지 않는다.
- 자동 배정은 추천 근거를 보여 준다: 직급 적합성, 역량 일치, 경험, 배정 후 예상 부하.
- 수동 배정도 같은 확인 모달을 사용한다.
- 담당자 변경·업무 수정·상태 전환은 기존 값을 덮어쓰지 않고 이력으로 남긴다.
- 상태 전환은 `변경/전환 이력`을 입력해야 저장된다. 빈 상태로 저장되는 버그를 재발시키지 않는다.
- 만족도는 해당 업무의 실제 담당자 본인만 입력한다.
- 업무부하율은 임의 입력하지 않고 현재 진행 중 업무와 개인별 가용시간·허용부하 설정으로 계산한다.
- 강조색은 `#0f4c81`, 보조 진한 색은 `#0b3860`, 배경은 `#e8eff5`를 사용한다.
- 모바일 앱·푸시 알림·메신저 연동은 60일 범위에서 제외한다. 대신 필요한 경우 앱 내부 알림만 고려한다.

## 6. 검증 체크리스트

- [ ] 새 업무 등록 후 목록에 즉시 표시
- [ ] Supabase 저장 후 새로고침해도 데이터 유지
- [ ] 숫자가 아닌 KPI 정량값 저장 차단
- [ ] KPI 기록 최신 입력이 목록 맨 위에 표시
- [ ] 팀장이 아닌 사용자는 업무 수정·배정 미정 전환 불가
- [ ] 상태 전환 이력 없이 상태 변경 불가
- [ ] 다중 담당자별 부하율과 만족도 권한이 올바름
- [ ] 자동 배정 추천 점수와 예상 부하가 일치
- [ ] 인수인계·의사결정·품질 이력 저장 및 조회
- [ ] 기간별 KPI CSV 다운로드 컬럼 확인
- [ ] `node --check app.js` 통과
- [ ] 로컬 서버 HTTP 200 및 주요 화면 브라우저 확인

## 7. 다음 세션 첫 행동

1. 이 문서와 [R&R_정리판_실사용_기획서.md](C:\Users\user\Documents\ChatGPT\원익Way IPS\R&R_정리판_실사용_기획서.md)를 읽는다.
2. `app.js`, `supabase.sql`, `index.html`, `styles.css`의 현재 상태를 확인한다.
3. 먼저 Supabase 연결 상태와 테이블 미비점을 점검한다.
4. 그 결과를 짧게 보고한 뒤, 영구 저장부터 구현한다.
